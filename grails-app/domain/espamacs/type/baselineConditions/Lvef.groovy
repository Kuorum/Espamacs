package espamacs.type.baselineConditions

import espamacs.type.DatabaseEnumType

/**
 * English LVEF => Left ventricular ejection fraction
 * Spanish FEVI => La fracción de expulsion del ventrículo izquierdo
 */
class Lvef extends DatabaseEnumType{

    static constraints = {
    }
}
