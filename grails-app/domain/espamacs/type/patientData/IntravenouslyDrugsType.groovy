package espamacs.type.patientData

import espamacs.type.DatabaseEnumType

/**
 * Spanish: ADVP => Adicto a las drogas via parental
 */
class IntravenouslyDrugsType extends DatabaseEnumType{

    static constraints = {
    }
}
